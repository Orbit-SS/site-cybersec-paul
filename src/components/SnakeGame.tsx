"use client";

export default function SnakeGame() {
  return (
    <iframe
      src="/snake.html"
      width={504}
      height={460}
      className="border-0 rounded-lg"
      title="Snake Game"
      style={{ background: "#0a0f0a" }}
    />
  );
}
